{
  "name": "ui-architecture-demo",
  "version": "1.0.0",
  "description": "White-label UI component library and theming engine — 16 reusable components driven by CSS custom properties across 3 themes",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "node server.js"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}